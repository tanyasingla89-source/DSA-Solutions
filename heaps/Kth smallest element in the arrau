// step 1: push first k elements
for(int i = 0; i < k; i++)
    pq.push(arr[i]);

// step 2: check remaining
for(int i = k; i < n; i++){
    if(arr[i] < pq.top()){
        pq.pop();
        pq.push(arr[i]);
    }
}
return pq.top();
